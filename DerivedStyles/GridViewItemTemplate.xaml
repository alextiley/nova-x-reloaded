<ResourceDictionary
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:p="clr-namespace:Playnite;assembly=Playnite"
  xmlns:popt="http://schemas.microsoft.com/winfx/2006/xaml/presentation/options"
  mc:Ignorable="d">

  <Style
    x:Key="VisualBrushBorderMask"
    popt:Freeze="True"
    TargetType="Border">
    <Setter Property="Background" Value="{DynamicResource GridViewItemMaskBackgroundBrush}" />
    <Setter Property="CornerRadius" Value="{DynamicResource GridViewCoverCornerRadius}" />
    <Setter Property="Visibility" Value="{Binding RelativeSource={RelativeSource Self}, Path=Tag, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
    <Style.Triggers>
      <MultiDataTrigger>
        <MultiDataTrigger.Conditions>
          <Condition Binding="{Settings ShowGridItemBackground}" Value="True" />
          <Condition Binding="{Settings ShowNamesUnderCovers}" Value="True" />
          <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="True" />
        </MultiDataTrigger.Conditions>
        <Setter Property="CornerRadius" Value="{DynamicResource GridViewCoverGridBackgroundDisabledCornerRadius}" />
      </MultiDataTrigger>
      <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="False">
        <Setter Property="CornerRadius" Value="0,0,0,0" />
      </DataTrigger>
    </Style.Triggers>
  </Style>

  <Style x:Key="GridViewItemTemplate" TargetType="{x:Type GameListItem}">
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type GameListItem}">
          <Border
            Background="Transparent"
            BorderBrush="{DynamicResource ButtonBackgroundBrush}"
            BorderThickness="{Settings GridItemMargin}"
            CornerRadius="{DynamicResource GridViewCoverCornerRadius}">

            <StackPanel
              Name="PART_PanelHost"
              Width="{Settings GridItemWidth}"
              d:DataContext="{x:Static DesignMainViewModel.DesignSelectedGameIntance}">
              <Grid>
                <Border
                  Name="Mask"
                  Style="{StaticResource VisualBrushBorderMask}"
                  Tag="{DynamicResource GridViewCoverUseRoundedCorners}" />
                <StackPanel>
                  <StackPanel.Style>
                    <Style TargetType="StackPanel">
                      <Style.Triggers>
                        <DataTrigger Binding="{Binding ElementName=Mask, Path=Tag}" Value="True">
                          <Setter Property="OpacityMask">
                            <Setter.Value>
                              <VisualBrush Visual="{Binding ElementName=Mask}" />
                            </Setter.Value>
                          </Setter>
                        </DataTrigger>
                      </Style.Triggers>
                    </Style>
                  </StackPanel.Style>
                  <ContentControl
                    x:Name="ThemeExtras_Banner"
                    Width="{Binding Width, ElementName=PART_ImageCover}"
                    HorizontalAlignment="Center"
                    RenderOptions.BitmapScalingMode="Fant"
                    Tag="{DynamicResource GridViewCoverDisplayLibraryBanner}"
                    Visibility="{Binding Tag, RelativeSource={RelativeSource Self}, Converter={StaticResource BooleanToVisibilityConverter}}" />
                  <Image
                    Name="PART_ImageCover"
                    Height="{Settings GridItemHeight}"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Top"
                    RenderOptions.BitmapScalingMode="Fant"
                    SnapsToDevicePixels="True"
                    Stretch="{Settings CoverArtStretch}"
                    StretchDirection="Both" />

                </StackPanel>


                <Border
                  x:Name="TextGameName"
                  Background="#99000000"
                  CornerRadius="{Binding ElementName=Mask, Path=CornerRadius}"
                  Visibility="Collapsed">
                  <TextBlock
                    Margin="5"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Style="{StaticResource BaseTextBlockStyle}"
                    Text="{Binding DisplayName}"
                    TextAlignment="Center"
                    TextTrimming="CharacterEllipsis"
                    TextWrapping="Wrap" />
                </Border>
                <Border
                  x:Name="BorderMouseOver"
                  Background="#000000"
                  CornerRadius="{Binding ElementName=Mask, Path=CornerRadius}"
                  Opacity="0.70"
                  Visibility="Collapsed" />
                <Border
                  x:Name="GlowPlayingBorder"
                  CornerRadius="{Binding ElementName=Mask, Path=CornerRadius}"
                  Style="{DynamicResource CoverIsPlayingAnimatedBackground}"
                  Tag="False"
                  Visibility="Collapsed" />
                <TextBlock
                  Name="EnablePlayingGlowBorder"
                  Tag="{DynamicResource GridViewEnablePlayingGlowBorder}"
                  Visibility="Collapsed" />
                <Border
                  Name="ShineBackground"
                  CornerRadius="{Binding ElementName=Mask, Path=CornerRadius}"
                  IsHitTestVisible="False"
                  Style="{DynamicResource AnimatedCoverShineBackground}"
                  Tag="False" />
                <TextBlock
                  Name="EnableHighlightAnimation"
                  Tag="{DynamicResource GridViewEnableCoverShineAnimation}"
                  Visibility="Collapsed" />

                <Border
                  x:Name="BorderDarkFade"
                  Background="{DynamicResource CoversButtonsBrush}"
                  CornerRadius="{Binding ElementName=Mask, Path=CornerRadius}"
                  Visibility="Collapsed" />
                <StackPanel>
                  <Border Height="{Binding ElementName=ThemeExtras_Banner, Path=ActualHeight}" Visibility="{Binding ElementName=ThemeExtras_Banner, Path=Visibility}" />
                  <TextBlock
                    Margin="8,5,5,5"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    FontFamily="Segoe MDL2 Assets"
                    FontSize="16"
                    Foreground="{DynamicResource TextBrush}"
                    Opacity="0.90"
                    Tag="{DynamicResource GridViewLibraryGameShowFavorite}"
                    Text="&#xE113;">
                    <TextBlock.Style>
                      <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                          <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                              <Condition Binding="{Binding Favorite}" Value="True" />
                              <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="True" />
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Visibility" Value="Visible" />
                            <Setter Property="Effect">
                              <Setter.Value>
                                <DropShadowEffect
                                  BlurRadius="6"
                                  RenderingBias="Performance"
                                  ShadowDepth="0"
                                  Color="#000000" />
                              </Setter.Value>
                            </Setter>
                          </MultiDataTrigger>
                        </Style.Triggers>
                      </Style>
                    </TextBlock.Style>
                  </TextBlock>
                </StackPanel>

                <Viewbox
                  Name="GameControls"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Bottom"
                  Visibility="Collapsed">
                  <StackPanel Orientation="Horizontal">
                    <Button
                      Name="PART_ButtonPlay"
                      Margin="20,0,2,12"
                      VerticalAlignment="Center"
                      Content="&#xE102;"
                      FontFamily="Segoe MDL2 Assets"
                      Style="{StaticResource SimpleButton}" />
                    <Button
                      Name="PART_ButtonInfo"
                      Margin="2,0,20,12"
                      VerticalAlignment="Center"
                      Content="&#xE946;"
                      FontFamily="Segoe MDL2 Assets"
                      Style="{StaticResource SimpleButton}" />
                  </StackPanel>
                </Viewbox>
                <!--  Steam-style 100% achievement badge  -->
                <ItemsControl Panel.ZIndex="99" ItemsSource="{Binding Game.Tags}">
                  <!--  We use a Grid panel so that if a game has 10 tags, the 9 hidden ones take up exactly 0 pixels of space  -->
                  <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                      <Grid />
                    </ItemsPanelTemplate>
                  </ItemsControl.ItemsPanel>

                  <ItemsControl.ItemTemplate>
                    <DataTemplate>
                      <!--  1. Default to Collapsed here  -->
                      <StackPanel
                        x:Name="RootPanel"
                        Margin="0,0,0,-23"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Bottom"
                        Orientation="Horizontal"
                        Visibility="Collapsed">

                        <Grid VerticalAlignment="Center">

                          <!--  YOUR BORDER  -->
                          <Border
                            Margin="20,-10,0,0"
                            VerticalAlignment="Center"
                            Background="#1fadff"
                            CornerRadius="0,4,4,0">
                            <!--  We wrap the text and the shadow together so they are the exact same size  -->
                            <Grid>
                              <TextBlock
                                Padding="25,2,8,3"
                                VerticalAlignment="Center"
                                FontSize="{DynamicResource FontSizeSmall}"
                                Foreground="#ffffff"
                                Text="100% Complete"
                                TextTrimming="CharacterEllipsis" />

                              <!--  NEW: FIXED BORDER SHADOW (Now stretches perfectly over the blue box)  -->
                              <Border
                                x:Name="DarkBorder"
                                Background="#80000000"
                                CornerRadius="0,4,4,0"
                                Visibility="Collapsed" />
                            </Grid>
                          </Border>

                          <!--  YOUR IMAGE  -->
                          <Image
                            Width="44"
                            Height="44"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            RenderOptions.BitmapScalingMode="Fant"
                            Source="{ThemeFile 'Images/ribbon.png'}"
                            Stretch="Uniform">
                            <Image.Effect>
                              <DropShadowEffect
                                BlurRadius="6"
                                Opacity="0.8"
                                RenderingBias="Performance"
                                ShadowDepth="2"
                                Color="#000000" />
                            </Image.Effect>
                          </Image>

                          <!--  NEW: IMAGE SHADOW  -->
                          <Rectangle
                            x:Name="DarkImage"
                            Width="44"
                            Height="44"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Fill="#40000000"
                            Visibility="Collapsed">
                            <Rectangle.OpacityMask>
                              <ImageBrush ImageSource="{ThemeFile 'Images/ribbon.png'}" Stretch="Uniform" />
                            </Rectangle.OpacityMask>
                          </Rectangle>

                        </Grid>
                      </StackPanel>

                      <!--  2. ALL LOGIC GOES HERE AT THE BOTTOM  -->
                      <DataTemplate.Triggers>
                        <!--  Make the whole ribbon visible if it has the tag  -->
                        <DataTrigger Binding="{Binding Name}" Value="[PA] Completed">
                          <Setter TargetName="RootPanel" Property="Visibility" Value="Visible" />
                        </DataTrigger>

                        <!--  If uninstalled, make the two black shadow overlays visible  -->
                        <MultiDataTrigger>
                          <MultiDataTrigger.Conditions>
                            <Condition Binding="{Settings DarkenUninstalledGamesGrid}" Value="True" />
                            <Condition Binding="{Binding DataContext.Game.IsInstalled, RelativeSource={RelativeSource AncestorType=ItemsControl}}" Value="False" />
                          </MultiDataTrigger.Conditions>
                          <Setter TargetName="DarkBorder" Property="Visibility" Value="Visible" />
                          <Setter TargetName="DarkImage" Property="Visibility" Value="Visible" />
                        </MultiDataTrigger>
                      </DataTemplate.Triggers>

                    </DataTemplate>
                  </ItemsControl.ItemTemplate>
                </ItemsControl>
              </Grid>

              <Border
                x:Name="ShowGameNameBorder"
                Background="Transparent"
                Visibility="{Settings ShowNamesUnderCovers}">
                <Border.Style>
                  <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="0" />
                    <Style.Triggers>
                      <DataTrigger Binding="{Binding ElementName=Mask, Path=Tag}" Value="True">
                        <Setter Property="CornerRadius" Value="{DynamicResource GridViewShowGameNameCornerRadius}" />
                      </DataTrigger>
                    </Style.Triggers>
                  </Style>
                </Border.Style>
                <TextBlock
                  x:Name="ShowGameName"
                  Grid.Row="1"
                  Grid.Column="0"
                  Width="{Settings GridItemWidth}"
                  Padding="2,5,2,5"
                  VerticalAlignment="Center"
                  Foreground="{DynamicResource TextBrush}"
                  Style="{DynamicResource BaseTextBlockStyle}"
                  Text="{Binding DisplayName}"
                  TextAlignment="Center"
                  TextTrimming="CharacterEllipsis" />
              </Border>

              <DockPanel Width="{Settings GridItemWidth}" Visibility="{DynamicResource GridViewCoverSubtitleVisibility}">
                <TextBlock
                  Margin="0,0,0,-2"
                  Padding="2,4,2,5"
                  VerticalAlignment="Center"
                  FontFamily="Segoe MDL2 Assets"
                  FontSize="{DynamicResource FontSizeSmall}"
                  Foreground="{DynamicResource GridViewCoverSubtitleBrush}"
                  Text="&#xE121;"
                  TextAlignment="Left" />
                <TextBlock
                  Padding="3,4,2,2"
                  VerticalAlignment="Center"
                  FontSize="{DynamicResource FontSizeSmall}"
                  Foreground="{DynamicResource GridViewCoverSubtitleBrush}"
                  Style="{DynamicResource BaseTextBlockStyle}"
                  Text="{Binding Playtime, Mode=OneWay, Converter={StaticResource PlayTimeToStringConverter}, IsAsync=True}"
                  TextAlignment="Left"
                  TextTrimming="CharacterEllipsis" />
              </DockPanel>
              <DockPanel Width="{Settings GridItemWidth}" Tag="{DynamicResource GridViewCoverCompStatusSubtitleVisibility}">
                <DockPanel.Style>
                  <Style TargetType="DockPanel">
                    <Setter Property="Visibility" Value="Visible" />
                    <Style.Triggers>
                      <DataTrigger Binding="{Binding CompletionStatus.Name, TargetNullValue='', FallbackValue=''}" Value="">
                        <Setter Property="Visibility" Value="Hidden" />
                      </DataTrigger>
                      <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="Collapsed">
                        <Setter Property="Visibility" Value="Collapsed" />
                      </DataTrigger>
                    </Style.Triggers>
                  </Style>
                </DockPanel.Style>

                <TextBlock
                  Margin="0,0,0,-2"
                  Padding="2,4,2,5"
                  VerticalAlignment="Center"
                  FontFamily="Segoe MDL2 Assets"
                  FontSize="{DynamicResource FontSizeSmall}"
                  Foreground="{DynamicResource GridViewCoverSubtitleBrush}"
                  Text="&#xE102;"
                  TextAlignment="Left" />
                <TextBlock
                  x:Name="CompletionStatusName"
                  Padding="3,4,2,2"
                  VerticalAlignment="Center"
                  FontSize="{DynamicResource FontSizeSmall}"
                  Foreground="{DynamicResource GridViewCoverSubtitleBrush}"
                  Style="{DynamicResource BaseTextBlockStyle}"
                  Text="{Binding CompletionStatus.Name, Mode=OneWay, IsAsync=True}"
                  TextAlignment="Left"
                  TextTrimming="CharacterEllipsis"
                  ToolTip="{Binding RelativeSource={RelativeSource Self}, Path=Text}" />
              </DockPanel>
            </StackPanel>
          </Border>

          <ControlTemplate.Triggers>
            <MultiDataTrigger>
              <MultiDataTrigger.Conditions>
                <Condition Binding="{Binding ElementName=EnableHighlightAnimation, Path=Tag}" Value="True" />
                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}" Value="True" />
              </MultiDataTrigger.Conditions>
              <Setter TargetName="ShineBackground" Property="Tag" Value="True" />
            </MultiDataTrigger>
            <MultiDataTrigger>
              <MultiDataTrigger.Conditions>
                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsVisible}" Value="True" />
                <Condition Binding="{Binding Source={x:Static p:PlayniteApplication.Current}, Path=IsActive}" Value="True" />
                <Condition Binding="{Binding ElementName=EnablePlayingGlowBorder, Path=Tag}" Value="True" />
                <Condition Binding="{Binding IsRunning}" Value="True" />
              </MultiDataTrigger.Conditions>
              <Setter TargetName="GlowPlayingBorder" Property="Tag" Value="True" />
              <Setter TargetName="GlowPlayingBorder" Property="Visibility" Value="Visible" />
            </MultiDataTrigger>
            <DataTrigger Binding="{Settings ShowGridItemBackground}" Value="True">
              <Setter TargetName="ShowGameName" Property="Padding" Value="3,5,3,5" />
              <Setter TargetName="ShowGameName" Property="TextAlignment" Value="Center" />
              <Setter TargetName="ShowGameNameBorder" Property="Background" Value="{DynamicResource ControlBackgroundBrush}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding ElementName=Mask, Path=Tag}" Value="True">
              <Setter TargetName="ShowGameNameBorder" Property="CornerRadius" Value="{DynamicResource GridViewShowGameNameCornerRadius}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding IsInstalled}" Value="False">
              <Setter TargetName="PART_ButtonPlay" Property="Content" Value="&#xE896;" />
            </DataTrigger>
            <MultiDataTrigger>
              <MultiDataTrigger.Conditions>
                <Condition Binding="{Binding IsInstalled}" Value="False" />
                <Condition Binding="{Settings DarkenUninstalledGamesGrid}" Value="True" />
              </MultiDataTrigger.Conditions>
              <Setter TargetName="BorderMouseOver" Property="Visibility" Value="Visible" />
            </MultiDataTrigger>
            <MultiDataTrigger>
              <MultiDataTrigger.Conditions>
                <Condition Binding="{Binding ElementName=BorderMouseOver, Path=Visibility}" Value="Visible" />
                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}" Value="True" />
              </MultiDataTrigger.Conditions>
              <MultiDataTrigger.EnterActions>
                <BeginStoryboard>
                  <Storyboard>
                    <DoubleAnimation
                      Storyboard.TargetName="BorderMouseOver"
                      Storyboard.TargetProperty="Opacity"
                      To="0.00"
                      Duration="0:0:0.30">
                      <DoubleAnimation.EasingFunction>
                        <CubicEase EasingMode="EaseOut" />
                      </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                  </Storyboard>
                </BeginStoryboard>
              </MultiDataTrigger.EnterActions>
              <MultiDataTrigger.ExitActions>
                <BeginStoryboard>
                  <Storyboard>
                    <DoubleAnimation
                      Storyboard.TargetName="BorderMouseOver"
                      Storyboard.TargetProperty="Opacity"
                      To="0.80"
                      Duration="0:0:0.30">
                      <DoubleAnimation.EasingFunction>
                        <CubicEase EasingMode="EaseOut" />
                      </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                  </Storyboard>
                </BeginStoryboard>
              </MultiDataTrigger.ExitActions>
            </MultiDataTrigger>
            <MultiDataTrigger>
              <MultiDataTrigger.Conditions>
                <Condition Binding="{Binding CoverImage}" Value="{x:Null}" />
                <Condition Binding="{Settings ShowNameEmptyCover}" Value="True" />
              </MultiDataTrigger.Conditions>
              <Setter TargetName="TextGameName" Property="Visibility" Value="Visible" />
            </MultiDataTrigger>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="BorderDarkFade" Property="Visibility" Value="Visible" />
              <Setter TargetName="GameControls" Property="Visibility" Value="Visible" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style
    x:Key="CoverIsPlayingAnimatedBackground"
    popt:Freeze="True"
    TargetType="Border">
    <Style.Resources>
      <Color x:Key="BackgroundColor1">#26ffffff</Color>
      <Color x:Key="BackgroundColor2">#00000000</Color>
      <Color x:Key="BackgroundColor3">#99000000</Color>
    </Style.Resources>
    <Setter x:Name="AnimatedBackground" Property="Background">
      <Setter.Value>
        <SolidColorBrush Color="{StaticResource BackgroundColor2}" />
      </Setter.Value>
    </Setter>
    <Style.Triggers>
      <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="True">
        <DataTrigger.EnterActions>
          <BeginStoryboard x:Name="Animation">
            <Storyboard Timeline.DesiredFrameRate="20">
              <ColorAnimationUsingKeyFrames
                AutoReverse="True"
                FillBehavior="HoldEnd"
                RepeatBehavior="Forever"
                Storyboard.TargetProperty="(Background).(SolidColorBrush.Color)">
                <ColorAnimationUsingKeyFrames.KeyFrames>
                  <LinearColorKeyFrame KeyTime="00:00:00.00" Value="{StaticResource BackgroundColor1}" />
                  <LinearColorKeyFrame KeyTime="00:00:00.10" Value="{StaticResource BackgroundColor1}" />
                  <LinearColorKeyFrame KeyTime="00:00:00.80" Value="{StaticResource BackgroundColor2}" />
                  <LinearColorKeyFrame KeyTime="00:00:01.50" Value="{StaticResource BackgroundColor3}" />
                  <LinearColorKeyFrame KeyTime="00:00:01.55" Value="{StaticResource BackgroundColor3}" />
                </ColorAnimationUsingKeyFrames.KeyFrames>
              </ColorAnimationUsingKeyFrames>
            </Storyboard>
          </BeginStoryboard>
        </DataTrigger.EnterActions>
        <DataTrigger.ExitActions>
          <RemoveStoryboard BeginStoryboardName="Animation" />
        </DataTrigger.ExitActions>
      </DataTrigger>
    </Style.Triggers>
  </Style>

  <Style
    x:Key="AnimatedCoverShineBackground"
    popt:Freeze="True"
    TargetType="Border">
    <Setter Property="Opacity" Value="0.00" />
    <Setter Property="Background">
      <Setter.Value>
        <LinearGradientBrush StartPoint="1.0, 0.0" EndPoint="0.07, 1.0">
          <GradientStop Offset="-0.10" Color="#00d2d2d2" />
          <GradientStop Offset="0.18" Color="#59d2d2d2" />
          <GradientStop Offset="0.22" Color="#59d2d2d2" />
          <GradientStop Offset="0.25" Color="Transparent" />
        </LinearGradientBrush>
      </Setter.Value>
    </Setter>
    <Style.Triggers>
      <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="True">
        <DataTrigger.EnterActions>
          <BeginStoryboard>
            <Storyboard Timeline.DesiredFrameRate="20">
              <DoubleAnimation
                Storyboard.TargetProperty="Opacity"
                To="0.90"
                Duration="0:0:0.5">
                <DoubleAnimation.EasingFunction>
                  <CubicEase EasingMode="EaseOut" />
                </DoubleAnimation.EasingFunction>
              </DoubleAnimation>
              <DoubleAnimation
                Storyboard.TargetProperty="Background.(GradientBrush.GradientStops)[1].(GradientStop.Offset)"
                To="0.35"
                Duration="0:0:0.5">
                <DoubleAnimation.EasingFunction>
                  <CubicEase EasingMode="EaseOut" />
                </DoubleAnimation.EasingFunction>
              </DoubleAnimation>
              <DoubleAnimation
                Storyboard.TargetProperty="Background.(GradientBrush.GradientStops)[2].(GradientStop.Offset)"
                To="0.40"
                Duration="0:0:0.5">
                <DoubleAnimation.EasingFunction>
                  <CubicEase EasingMode="EaseOut" />
                </DoubleAnimation.EasingFunction>
              </DoubleAnimation>
              <DoubleAnimation
                Storyboard.TargetProperty="Background.(GradientBrush.GradientStops)[3].(GradientStop.Offset)"
                To="0.48"
                Duration="0:0:0.5">
                <DoubleAnimation.EasingFunction>
                  <CubicEase EasingMode="EaseOut" />
                </DoubleAnimation.EasingFunction>
              </DoubleAnimation>
            </Storyboard>
          </BeginStoryboard>
        </DataTrigger.EnterActions>
        <DataTrigger.ExitActions>
          <BeginStoryboard>
            <Storyboard>
              <DoubleAnimation
                Storyboard.TargetProperty="Opacity"
                To="0.00"
                Duration="0:0:0.4">
                <DoubleAnimation.EasingFunction>
                  <CubicEase EasingMode="EaseOut" />
                </DoubleAnimation.EasingFunction>
              </DoubleAnimation>
              <DoubleAnimation
                Storyboard.TargetProperty="Background.(GradientBrush.GradientStops)[1].(GradientStop.Offset)"
                To="0.18"
                Duration="0:0:0.4">
                <DoubleAnimation.EasingFunction>
                  <CubicEase EasingMode="EaseOut" />
                </DoubleAnimation.EasingFunction>
              </DoubleAnimation>
              <DoubleAnimation
                Storyboard.TargetProperty="Background.(GradientBrush.GradientStops)[2].(GradientStop.Offset)"
                To="0.22"
                Duration="0:0:0.4">
                <DoubleAnimation.EasingFunction>
                  <CubicEase EasingMode="EaseOut" />
                </DoubleAnimation.EasingFunction>
              </DoubleAnimation>
              <DoubleAnimation
                Storyboard.TargetProperty="Background.(GradientBrush.GradientStops)[3].(GradientStop.Offset)"
                To="0.25"
                Duration="0:0:0.4">
                <DoubleAnimation.EasingFunction>
                  <CubicEase EasingMode="EaseOut" />
                </DoubleAnimation.EasingFunction>
              </DoubleAnimation>
            </Storyboard>
          </BeginStoryboard>
        </DataTrigger.ExitActions>
      </DataTrigger>
    </Style.Triggers>
  </Style>

  <Style
    x:Key="DuplicateHider_IconContentControlStyle1"
    BasedOn="{StaticResource {x:Type ContentControl}}"
    TargetType="ContentControl">
    <Setter Property="BorderBrush" Value="{x:Null}" />
    <Setter Property="Foreground" Value="{x:Null}" />
    <Setter Property="Background" Value="{x:Null}" />
    <Setter Property="Padding" Value="0,0,0,0" />
    <Setter Property="Margin" Value="0,0,0,0" />
    <Setter Property="BorderThickness" Value="0,0,0,0" />
    <Setter Property="HorizontalAlignment" Value="Stretch" />
    <Setter Property="VerticalAlignment" Value="Center" />
    <Setter Property="ClipToBounds" Value="False" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate>
          <Grid>
            <Border
              x:Name="IconBackground"
              Background="{DynamicResource OpaqueGrayHighlightBrush}"
              CornerRadius="8"
              Opacity="0.0"
              Visibility="Hidden" />
            <Image
              Margin="3"
              Opacity="0.95"
              RenderOptions.BitmapScalingMode="Fant"
              Source="{Binding Icon}" />
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="IconBackground" Property="Border.Visibility" Value="Visible" />
              <Setter TargetName="IconBackground" Property="Border.Background" Value="{DynamicResource OpaqueGrayHighlightBrush}" />
              <Setter TargetName="IconBackground" Property="Border.Opacity" Value="0.75" />
            </Trigger>
            <DataTrigger Binding="{Binding IsCurrent}" Value="True">
              <Setter TargetName="IconBackground" Property="Border.Visibility" Value="Visible" />
              <Setter TargetName="IconBackground" Property="Border.Background" Value="{DynamicResource OpaqueGrayHighlightBrush}" />
              <Setter TargetName="IconBackground" Property="Border.Opacity" Value="1" />
            </DataTrigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style
    x:Key="DropShadowBorderStyle"
    popt:Freeze="True"
    TargetType="Border">
    <Setter Property="BorderBrush" Value="{DynamicResource GridViewItemMaskBackgroundBrush}" />
    <Setter Property="BorderThickness" Value="8,0,8,8" />
    <Setter Property="CornerRadius" Value="{DynamicResource GridViewCoverCornerRadius}" />
    <Setter Property="CacheMode">
      <Setter.Value>
        <BitmapCache
          EnableClearType="False"
          RenderAtScale="0.5"
          SnapsToDevicePixels="False" />
      </Setter.Value>
    </Setter>
    <Setter Property="Effect">
      <Setter.Value>
        <DropShadowEffect
          BlurRadius="8"
          Direction="270"
          Opacity="0.5"
          RenderingBias="Performance"
          ShadowDepth="5"
          Color="#000000" />
      </Setter.Value>
    </Setter>
    <Style.Triggers>
      <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="False">
        <Setter Property="CornerRadius" Value="0,0,0,0" />
      </DataTrigger>
    </Style.Triggers>
  </Style>
</ResourceDictionary>
